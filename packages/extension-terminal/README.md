# Terminal

Run shell commands in a dedicated Yank Note sidebar terminal.

## Features

- Open a shell directly from the file tab bar
- Switch between embedded, floating, and maximized panel modes
- Save custom quick commands on the welcome screen and run them with one click

## Requirements

- Yank Note with terminal support enabled
